# Task Matrix

بورد کانبان سه‌ستونه (برنامه‌ریزی‌شده / در حال انجام / انجام‌شده) با تم دارک نئون ماتریکسی، افزودن و حذف تسک، و جابجایی با درگ‌اند‌دراپ.

## اجرا

ساده‌ترین راه، باز کردن مستقیم فایل است:

```bash
start index.html
```

برای پایداری بیشتر (فونت‌ها و localStorage) پیشنهاد می‌شود با یک سرور محلی سبک اجرا شود:

```bash
npx serve .
```

یا در VS Code از افزونه‌ی Live Server استفاده کنید (کلیک راست روی `index.html` → Open with Live Server).

## ذخیره‌سازی

هر کاربر با ایمیل و رمز ثبت‌نام می‌کند و بورد او در Netlify Blobs (استور `boards`) ذخیره می‌شود، پس روی همه‌ی دستگاه‌ها یکسان است. اطلاعات کاربران (با رمز هش‌شده با scrypt) در استور `users` است. توابع سرور در `netlify/functions` و کد مشترک احراز هویت در `netlify/lib/auth.js` قرار دارد. متغیر محیطی `AUTH_SECRET` برای امضای نشست لازم است.

ورود با گوگل از OAuth استفاده می‌کند و به متغیرهای `GOOGLE_CLIENT_ID` و `GOOGLE_CLIENT_SECRET` نیاز دارد. آدرس بازگشت (redirect URI) که در Google Cloud Console ثبت می‌شود: `https://<دامنه‌ی سایت>/api/auth/google/callback`. اولین ورود با گوگل به حسابی که قبلاً با رمز ساخته شده، رمز آن حساب را باطل می‌کند، چون ثبت‌نام با رمز مالکیت ایمیل را تأیید نمی‌کند.

«فراموشی رمز» یک لینک یک‌بارمصرف ۳۰ دقیقه‌ای از طریق SMTP می‌فرستد و با هر سرویسی که SMTP دارد (مثل Gmail یا Yandex Mail) کار می‌کند. متغیرهای لازم: `SMTP_HOST`، `SMTP_PORT` (پیش‌فرض 465)، `SMTP_USER` (که آدرس فرستنده هم هست) و `SMTP_PASS`. برای Gmail: `smtp.gmail.com`، پورت `465` و یک App Password (نیازمند تأیید دومرحله‌ای). برای Yandex: `smtp.yandex.com`، پورت `465` و یک App Password.

محدودیت تلاش: ۵ ورود ناموفق برای یک ایمیل یا ۲۰ ورود ناموفق از یک آی‌پی، ۱۵ دقیقه قفل می‌کند. درخواست بازیابی هم به ۳ بار برای هر ایمیل و ۱۰ بار برای هر آی‌پی در ۱۵ دقیقه محدود است.

اگر قبلاً تسک‌هایی در `localStorage` مرورگر بوده، هنگام ثبت‌نام به حساب جدید منتقل می‌شوند.

## توسعه

برای تغییرات بعدی (رنگ‌بندی، افکت‌ها، قابلیت‌های جدید مثل برچسب یا مهلت انجام)، همین‌جا از ترمینال Claude Code داخل VS Code درخواست بدهید؛ کل اپ در یک فایل `index.html` (HTML/CSS/JS خالص، بدون build tool) قرار دارد.
